# Arch

## pacman
* Search database for regex or keyword:  `pacman -Ss {search-pattern}`
* Install package:  `pacman -S {package-name}`
* Remove package and its dependencies:  `pacman -Rs {package-name}`
* Synchronize and update all installed packages:  `pacman -Syu`

